{{#each model as |entry|}}
  {{conversation-entry
    conversation=entry.conversation
    message=entry.messagePreview
    companyName="@Cool Company"
    onArrowClicked=(action 'onArrowClicked')
  }}
{{/each}}
{{outlet}}
