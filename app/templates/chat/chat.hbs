{{#each model as |entry|}}
  {{conversation-entry imageSrc=entry.conversation.employee.image
                       message=entry.messagePreview
                       personName=entry.conversation.employee.name}}
{{/each}}
{{outlet}}
